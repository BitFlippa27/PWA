import {
    DATA_LOADED,
    DATALOAD_FAILED,
    DATA_INSERTED,
    DATA_CHANGED,
    DATA_REMOVED,
    INSERTED_OFFLINE,
    CHANGED_OFFLINE,
    REMOVED_OFFLINE,
    CONCURRENCY
    } from "../actions/types";

const initialState = [];